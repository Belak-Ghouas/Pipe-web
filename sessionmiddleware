

exports.sessionState = function (req, res, next) {
   
   const userId = req.session.userId
    if(userId){
        return next()
    }else{
        req.session.destroy()
         return res.redirect('/login')
    }
}